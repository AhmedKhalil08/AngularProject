import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-register-type',
  imports: [RouterLink],
  templateUrl: './register-type.html',
  styleUrl: './register-type.css',
})
export class RegisterType {}
