import { DateCleanPipe } from './date-clean-pipe';

describe('DateCleanPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCleanPipe();
    expect(pipe).toBeTruthy();
  });
});
