import { ApplyPromoPipe } from './apply-promo-pipe';

describe('ApplyPromoPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyPromoPipe();
    expect(pipe).toBeTruthy();
  });
});
